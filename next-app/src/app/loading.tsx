
/**
 * A fallback component to display when the page is loading. 
 * It displays a simple "Loading..." message.
 */
export default function Loading() {
  return (
    <div>
      {/* Display a simple "Loading..." message. */}
      Loading...  
    </div>
  );
}